# Output from manual local generator runs (docs/ itself IS committed)
docs/output/

# Materialised SchemaTracker data: an on-demand shallow clone of the `latest`
# branch, tens of MB, never committed. CI re-clones it on every run.
upstream/schema-tracker/

# Python cache
docs/__pycache__/
__pycache__/

# Astro site (site/ itself IS committed)
site/node_modules/
site/dist/
site/.astro/
site/public/generated/
